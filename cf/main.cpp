#include<cstdio>
#include<iostream>
#include<vector>
#include <cstring>
#include<map>
#include<set>
#include <algorithm>
#include <stack>
#include <queue>
#include <array>
#include <cmath>

#define CLOSEIO ios::sync_with_stdio(false)
#define pf printf
#define fi first
#define se second
#define sf scanf
#define SZ(x) ((int)(x).size())
#define all(x) (x).begin(),(x).end()
#define rep(a,b,c) for(int (a)=(b);(a)<(c);++(a))
#define drep(a,b,c) for(int (a)=(b)-1;(a)>=(c);--(a))
#define PI acos(-1)
#define eps 1e-8
#define pb(x) push_back(x)
#define mp(x, y) make_pair(x, y)
#define dbg(x) cout << #x << " = " << x << endl
#define mst(a, x) memset(a, x, sizeof(a))
using namespace std;
typedef int64_t LL;
typedef double db;
typedef vector<int> VI;
typedef pair<int, int> PII;
const int maxn = 1e5+100;
const int inf = 1e9+100;
LL mod = 1000000007;

int main(int argc, char *argv[]) {
  cout << std::pow(10, 20) + std::pow(10, -20) << endl;
  return 0;
}
